    <!-- THIRST KEEPER LOGO -->

    <img src="images/thirst-keeper.png" class="img-fluid" alt="Responsive image">


    {{#if message.length}}
      {{message}}
    {{/if}}


    <!-- LOGIN FORM -->
    <form action="/login" method="post">
        <div>
            <label>Email</label>
            <input type="text"  name="email">
        </div>
        <div>
            <label>Password</label>
            <input type="password" name="password">
        </div>

        <button type="submit">Login</button>
    </form>
